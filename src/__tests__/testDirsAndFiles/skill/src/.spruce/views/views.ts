import BookSkillViewController from '../../skillViewControllers/Book.svc'
import BookViewController from '../../viewControllers/Book.vc'

const vcs = {
    BookSkillViewController,
    BookViewController
}

console.log(vcs)

export default vcs