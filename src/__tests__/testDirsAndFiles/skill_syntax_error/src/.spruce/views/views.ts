import BookSkillViewController from '../../skillViewControllers/Book.svc'
import BookViewController from '../../viewControllers/BookForm.vc'

const vcs = {
    BookSkillViewController,
    BookViewController
}

console.log(vcs)

const const var var wakawaka = 'test' go true

export default vcs